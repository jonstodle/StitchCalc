<?xml version="1.0" encoding="utf-8" ?>
<rxui:ReactiveContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:rxui="clr-namespace:ReactiveUI.XamForms;assembly=ReactiveUI.XamForms"
			 xmlns:vms="clr-namespace:StitchCalc.ViewModels"
			 x:TypeArguments="vms:HomeViewViewModel"
             x:Class="StitchCalc.Views.HomeView"
						 Icon="home.png"
						 Title="Products">

	<ContentPage.ToolbarItems>
		<ToolbarItem x:Name="AddProductToolbarItem" Text="Add">
			<ToolbarItem.Icon>
				<OnPlatform x:TypeArguments="FileImageSource" Android="add.png" WinPhone="add.png"/>
			</ToolbarItem.Icon>
		</ToolbarItem>
	</ContentPage.ToolbarItems>
	
	<StackLayout>
		<SearchBar x:Name="ProductSearchBar"/>

		<ListView x:Name="ProductListView">
			<ListView.ItemTemplate>
				<DataTemplate>
					<TextCell Text="{Binding Name}">
<!--					<TextCell Text="{Binding Name}" Detail="{Binding TotalPrice, StringFormat='{}{0:N2}'}">-->
						<TextCell.ContextActions>
							<!--<MenuItem Text="Delete" IsDestructive="True" Command="{Binding Delete}"/>-->
						</TextCell.ContextActions>
					</TextCell>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
	</StackLayout>
</rxui:ReactiveContentPage>